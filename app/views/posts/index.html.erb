<% @posts.each do |post| %>
	<div class="genie-box text-center col-xs-7 col-xs-offset-2 col-sm-3 col-sm-offset-2">
		<h1>
		<%= post.title %>
	</h1>
	<%= image_tag post.image, :class=> 'img-responsive' %>
	<%= post.description %>
	

</div>

<% end %>